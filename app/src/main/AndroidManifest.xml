<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bestcode95.educationaknow">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/launcher_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name=".utils.network.MyApplication"
        android:theme="@style/AppTheme">
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.MainActivity"/>
        <activity android:name=".main.ChoseCity"/>
        <activity android:name=".main.SearchDetail"/>
        <activity android:name=".main.SearchDetailSecond"/>
        <activity android:name=".main.MixedArea"/>
        <activity android:name=".main.SchoolOut"/>
        <activity android:name=".main.Edu"/>
        <activity android:name=".main.Discount"/>
        <activity android:name=".main.AfterClassActivity"/>
    </application>

</manifest>
